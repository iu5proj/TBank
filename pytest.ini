[pytest]
asyncio_mode = auto
testpaths =
    backend/tests
pythonpath =
    backend
norecursedirs =
    back
    backend_codex_initial_*
    infra
    llm_service
    parser_worker
    .venv
    .pytest_cache
    .ruff_cache

